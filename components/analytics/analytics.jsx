// import activateViewService from "../../services/analyticsService";
import React from 'react';

const Analtyics = ({ location: { pathname, search } }) => {
  // activateViewService(pathname + search);
  return null;
};

export default Analtyics;
